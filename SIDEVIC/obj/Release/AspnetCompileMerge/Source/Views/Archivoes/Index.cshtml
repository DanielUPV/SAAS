@model IEnumerable<SIDEVIC.Models.Archivo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_ruta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Indicador.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_ruta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Indicador.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_archivo }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_archivo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_archivo })
        </td>
    </tr>
}

</table>
