@model IEnumerable<SIDEVIC.Models.Evaluacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.evaluacion1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Indicador.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.evaluacion1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Indicador.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_evaluacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_evaluacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_evaluacion })
        </td>
    </tr>
}

</table>
